[project]
name = "metadev-protocol"
version = "0.1.0"
description = "Systeme de templates pour bootstrapper des projets Python assistes par IA"
readme = "README.md"
authors = [
    { name = "Vincent", email = "vincent.lamy.33@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "copier>=9.14.1",
    "pre-commit>=4.2.0",
    "ruff>=0.11.0",
]

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
